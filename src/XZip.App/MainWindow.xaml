<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="XZip.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winex="using:WinUIEx"
    xmlns:converters="using:XZip.App.Converters"
    mc:Ignorable="d"
    x:Uid="MainWindow"
    MinWidth="900"
    MinHeight="600">

    <Grid x:Name="RootGrid" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid x:Name="AppTitleBar"
              Grid.Row="0"
              Height="40"
              Background="Transparent"
              Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0"
                   Source="ms-appx:///Assets/AppIcon.png"
                   Width="20" Height="20"
                   VerticalAlignment="Center" />

            <TextBlock x:Uid="MainWindow_AppTitle"
                       Grid.Column="1"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </Grid>

        <!-- Navigation -->
        <NavigationView x:Name="Nav"
                        Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="Left"
                        OpenPaneLength="220"
                        IsPaneToggleButtonVisible="True"
                        SelectionChanged="Nav_SelectionChanged"
                        IsTitleBarAutoPaddingEnabled="False">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="MainWindow_NavOpen" Tag="open" Icon="OpenFile" />
                <NavigationViewItem x:Uid="MainWindow_NavCreate" Tag="create" Icon="Add" />
                <NavigationViewItem x:Uid="MainWindow_NavRecent" Tag="recent" Icon="ImportAll" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Uid="MainWindow_NavGitHub" Tag="github" Icon="Like" />
                <NavigationViewItem x:Uid="MainWindow_NavSettings" Tag="settings" Icon="Setting" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>

</winex:WindowEx>
