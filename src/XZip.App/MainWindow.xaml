<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="XZip.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winex="using:WinUIEx"
    xmlns:converters="using:XZip.App.Converters"
    mc:Ignorable="d"
    Title="XZip"
    MinWidth="900"
    MinHeight="600">

    <Grid x:Name="RootGrid" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid x:Name="AppTitleBar"
              Grid.Row="0"
              Height="40"
              Background="Transparent"
              Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0"
                   Source="ms-appx:///Assets/AppIcon.png"
                   Width="20" Height="20"
                   VerticalAlignment="Center" />

            <TextBlock Grid.Column="1"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Text="XZip" />
        </Grid>

        <!-- Navigation -->
        <NavigationView x:Name="Nav"
                        Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="Left"
                        OpenPaneLength="220"
                        IsPaneToggleButtonVisible="True"
                        SelectionChanged="Nav_SelectionChanged"
                        IsTitleBarAutoPaddingEnabled="False">

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="open" Content="Открыть" Icon="OpenFile" />
                <NavigationViewItem Tag="create" Content="Создать" Icon="Add" />
                <NavigationViewItem Tag="recent" Content="Недавние" Icon="ImportAll" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Tag="settings" Content="Настройки" Icon="Setting" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>

</winex:WindowEx>
